export enum ResultStatus {
  SUCCESS = 0,
  NOT_FOUND = 1,
  FORBIDDEN = 2,
}

export enum HTTP_STATUSES {
  SUCCESS = 200,
  CREATED = 201,
  NO_CONTENT = 204,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  INTERNAL_ERROR = 500,
  TOO_MANY_REQUESTS = 429,
}
